const toggle=document.querySelector('.menu-toggle');const links=document.querySelector('.nav-links');if(toggle){toggle.addEventListener('click',()=>links.classList.toggle('open'))}document.querySelectorAll('.product button').forEach(btn=>btn.addEventListener('click',()=>{btn.textContent='Added ✓';setTimeout(()=>btn.textContent='Add to Cart',1400)}));const obs=new IntersectionObserver(e=>e.forEach(x=>{if(x.isIntersecting)x.target.classList.add('show')}));document.querySelectorAll('.product,.feature,.service-list div,.card').forEach(el=>obs.observe(el));
