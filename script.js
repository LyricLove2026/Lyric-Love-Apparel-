const menu=document.querySelector('.menu-toggle');const links=document.querySelector('.nav-links');if(menu&&links){menu.addEventListener('click',()=>links.classList.toggle('open'))}document.querySelectorAll('.product button').forEach(b=>b.addEventListener('click',()=>{b.textContent='Added ✓';setTimeout(()=>b.textContent='Add to Cart',1200)}));const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.product,.feature,.service-list div').forEach(el=>obs.observe(el));
