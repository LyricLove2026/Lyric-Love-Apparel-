document.querySelectorAll('.mini-btn').forEach(btn=>btn.addEventListener('click',()=>{let c=document.querySelector('.cart');let n=Number(c.dataset.count||0)+1;c.dataset.count=n;c.textContent='🛒 Cart '+n;}));
